import HeroText from "../components/HeroText";
import HeroImg from "../components/(hero)/HeroImg";

const Hero = () => {
  return (
    <div className="Hero">
      <HeroText />
      <HeroImg />
    </div>
  );
};

export default Hero;
